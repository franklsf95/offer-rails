=simple_form_for :session, url: sessions_path, html: {class: 'form-horizontal'} do |f|
  =field_set_tag do
    =f.input_field :email, placeholder: 'E-mail'
    =f.input_field :password, placeholder: '密码'
    =f.button :submit, '登录', class: 'btn-primary'
    =link_to '新用户注册', prepare_users_path
